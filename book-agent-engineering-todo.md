# AI Book-Writing Agent 工程 TODO 清单

> 用途：后续开发进度确认。状态建议使用 `[ ]` 未开始、`[/]` 进行中、`[x]` 已完成、`[!]` 阻塞。

---

## 0. 项目基础

- [/] 初始化后端项目结构：`app/`、`domain/`、`services/`、`repositories/`、`workers/`、`tests/`。
- [/] 初始化前端项目结构：创建向导、写作控制台、预览编辑、审计报告、快照恢复、导出发布。
- [/] 选择并固定包管理方案、格式化工具、lint、类型检查、测试命令。
- [ ] 定义配置系统：LLM Provider、数据库、向量库、快照路径、任务队列参数、审计阈值。
- [ ] 建立开发环境启动脚本：后端、前端、本地队列 worker、测试数据库。
- [ ] 建立基础日志规范：request id、job id、project id、chapter no、LLM 调用成本。

## 1. 核心领域模型

- [x] 实现 `BookProject` 聚合根。
- [/] 实现 `CreationParams`、`WorldSetting`、`Character`、`PlotStructure`、`ChapterOutline`、`Chapter`。
- [ ] 实现 `Foreshadow` 伏笔模型，包含跨卷字段和关联伏笔链。
- [x] 实现 `ThemeTracker` 容器，支持按 `table_id` 读取追踪表。
- [x] 实现 `TrackingTableDef`、`TrackingColumnDef`、`TablePatch`、`TableAuditResult`。
- [x] 实现 `AuditFinding`、`AuditReport`，包含 `compute_overall_score()`、`compute_overall_status()`。
- [/] 实现 `Checkpoint`、`CheckpointPayload`、`ChapterEvent`。
- [ ] 实现 `VolumeMemorySpace`、`VolumeBridge`、`SummaryNode`。

## 2. 体裁与题材策略接口

- [ ] 定义 `IGenreEngine` 抽象接口。
- [/] 定义 `IThemeStrategy` 抽象接口。
- [/] 实现小说体裁 `FictionGenreEngine`。
- [x] 实现推理 `MysteryThemeStrategy`。
- [x] 实现科幻 `SciFiThemeStrategy`。
- [x] 实现玄幻 `XuanhuanThemeStrategy`。
- [x] 为三种题材输出稳定的 `TrackingTableDef`，确保每张表有 `table_id`、主键、唯一键和合并策略。
- [ ] 实现 `build_chapter_audit_prompt(chapter, audit_context)`，禁止在审计引擎里硬编码题材 prompt。
- [ ] 实现 `update_tracking_tables()`，只接受已校验的 `TablePatch[]`。

## 3. 创建向导与参数系统

- [/] 实现通用参数表单：书名、核心创意、目标读者、语言风格、叙事视角、基调、特殊要求。
- [ ] 实现 LLM 建议参数：书名、时代背景、结局走向、情感曲线。
- [/] 实现题材专属参数动态加载。
- [ ] 实现 `llm_generate_or_custom` 控件：AI 生成、重新生成、用户自定义、基于反馈优化。
- [ ] 实现参数确认总览和回退修改。
- [/] 实现篇幅档位选择：微型、短篇、中篇、长篇、超长篇。
- [ ] 确认篇幅边界归类：50-100 万字归长篇，100 万字及以上归超长篇。

## 4. 后台任务队列

- [x] 实现 `WritingJob` 数据模型。
- [/] 实现本地任务队列：提交、查询、暂停、继续、取消、重试。
- [ ] 实现同项目写状态任务互斥锁。
- [ ] 实现只读任务并发：预览、查看报告、全局扫描查询。
- [x] 为每个写任务生成 `idempotency_key`。
- [/] 任务失败时写入错误信息、失败步骤、可重试标记。
- [ ] GUI 展示任务状态、当前步骤、进度、错误和重试入口。

## 5. LLM Provider 与结构化输出

- [ ] 定义 LLM Provider 抽象层。
- [ ] 支持至少一个实际 Provider。
- [ ] 实现结构化输出校验：schema 校验、字段缺失处理、非法枚举处理。
- [ ] 实现调用重试策略：超时、限流、网络错误、模型错误。
- [ ] 记录每次调用的模型、prompt hash、token、费用估算、耗时。
- [ ] 保存关键 prompt 与输出 hash，支撑审计复现。

## 6. 章节生成流水线

- [/] 实现世界观构建。
- [/] 实现角色设计。
- [/] 实现大纲生成。
- [x] 实现逐章写作 `write_chapter()`。
- [ ] 构建 `WritingContext`：L0 即时上下文、L1 短期记忆、L2 结构记忆、L3 检索记忆。
- [x] 实现章节生成后的审计触发。
- [ ] 实现自动修订循环，最多 2 次。
- [ ] 实现人工处理队列：连续失败、blocking fail、用户要求人工确认。

## 7. 通用审计系统

- [ ] 实现角色一致性检查。
- [ ] 实现时间线一致性检查。
- [ ] 实现空间一致性检查。
- [ ] 实现情节逻辑检查。
- [ ] 实现叙事质量检查。
- [x] 实现通用检查结果到 `AuditFinding` 的映射。
- [ ] 实现 blocking 规则：角色生死、时间线硬矛盾、核心规则破坏。
- [x] 实现 warning 阈值配置。

## 8. 题材专属审计

- [x] 推理：实现线索追踪表。
- [x] 推理：实现嫌疑人状态表。
- [x] 推理：实现作案时间线对照表。
- [x] 推理：实现诡计完整性表。
- [x] 推理：实现推理逻辑链表。
- [x] 科幻：实现科技设定表。
- [x] 科幻：实现社会形态表。
- [x] 科幻：实现物理/宇宙规则表。
- [x] 科幻：实现文明档案表。
- [x] 科幻：实现哲学探讨进度表。
- [x] 玄幻：实现修炼进度表。
- [x] 玄幻：实现势力格局表。
- [x] 玄幻：实现物品/资源追踪表。
- [x] 玄幻：实现任务/使命/恩怨追踪表。
- [x] 玄幻：实现爽点节奏监控表。
- [x] 玄幻：实现金手指使用记录表。
- [x] 玄幻：实现世界观规则检测表。
- [x] 所有题材：LLM 只输出 `TablePatch[]`，程序负责校验和落库。
- [/] 所有题材：实现补丁幂等校验和版本冲突处理。

## 9. 审计报告与修订

- [/] 实现综合审计报告：通用检查、题材检查、伏笔检查、总分、状态、决策。
- [x] 实现 pass/warning/fail 分级展示。
- [/] 实现追踪表差异展示：新增、修改、删除、冲突。
- [ ] 实现“生成修订稿”操作。
- [ ] 实现“接受 warning 继续”操作，写入事件日志。
- [ ] 实现“标记人工处理”操作。
- [x] 实现报告随章节快照绑定保存。

## 10. 快照与恢复

- [x] 实现章节快照：最近 N 章滚动保留。
- [ ] 实现 arc/volume/manual 快照永久保留。
- [ ] 实现 emergency 快照。
- [/] 实现快照原子写入：payload、元数据、索引版本必须一致。
- [/] 实现 `content_hash`、`style_anchor_hash` 校验。
- [ ] 实现快照 migration。
- [ ] 实现恢复前差异摘要：将丢弃章节、回滚追踪表、覆盖人工编辑、恢复起点。
- [ ] 实现四层恢复上下文：风格锚定、结构摘要、叙事状态、即时上下文。
- [ ] 实现续写探针。
- [ ] 实现恢复验证分段处理：ready、manual_confirm、rebuild_context、manual_review。

## 11. 事件日志与人工编辑

- [/] 所有状态变更写入 `ChapterEvent`。
- [/] 实现事件幂等应用。
- [ ] 实现从最近可信快照重放事件。
- [ ] 手动编辑历史章节后生成 manual 快照。
- [ ] 手动编辑后标记 dirty range。
- [ ] dirty range 触发摘要链重建。
- [ ] dirty range 触发向量索引重建。
- [ ] dirty range 触发追踪表和审计报告重算。

## 12. 风格锚定与校准

- [ ] 实现 `StyleAnchor`。
- [ ] 实现 `StyleCalibrationProfile`。
- [ ] 抽取样本文段：最近章节、里程碑章节、用户标记章节。
- [ ] 计算句长分布、段落长度、对白比例、视角标记、角色口癖。
- [ ] LLM 复核风格偏差并输出解释。
- [ ] 用户接受的风格变化写回校准 profile。
- [ ] 模型切换后强制执行续写探针和风格验证。

## 13. 长跨度对齐

- [ ] 实现向量检索。
- [ ] 实现结构化查询。
- [ ] 实现摘要链追溯。
- [ ] 实现 `LongRangeAlignmentService.rank_and_merge()`。
- [ ] 实现可信度排序：结构化表 > 原文证据 > 摘要 > 自动推断。
- [ ] 实现角色存活全局扫描。
- [ ] 实现时间线全局扫描。
- [ ] 实现伏笔超期扫描。
- [ ] 实现设定漂移扫描。
- [ ] 实现风格漂移扫描。
- [ ] 实现题材节奏扫描。

## 14. 分卷隔离与 VolumeBridge

- [ ] 实现 `VolumeMemorySpace`。
- [ ] 当前卷默认只检索当前卷命名空间。
- [ ] 旧卷内容仅通过 VolumeBridge、全局设定、全局角色状态或显式检索进入上下文。
- [ ] 卷结束后封存当前卷追踪表。
- [ ] 生成 `VolumeBridge`。
- [ ] 桥接审计：检查遗漏的角色、伏笔、物品、规则和禁忌事实。
- [ ] 长篇卷末建议人工确认。
- [ ] 超长篇卷末强制人工确认。
- [ ] 用户确认后的 VolumeBridge 写入事件日志。
- [ ] 修改旧卷后重新生成 VolumeBridge，并标记后续卷需重新对齐。

## 15. GUI 页面

- [x] 创建向导页面。
- [x] 写作控制台页面。
- [/] 章节预览/编辑页面。
- [x] 审计报告页面。
- [/] 追踪表查看与差异页面。
- [x] 断点快照列表页面。
- [ ] 恢复差异确认页面。
- [/] 后台任务状态面板。
- [ ] VolumeBridge 确认页面。
- [/] 导出页面。

## 16. 导出

- [x] 导出 Markdown 草稿。
- [ ] 导出 `.docx`。
- [ ] 导出 `.pdf`。
- [x] 导出审计报告。
- [x] 导出项目快照。
- [x] 导出追踪表。
- [ ] 导出 VolumeBridge。

## 17. 测试与验收

- [/] 建立推理审计 fixture。
- [/] 建立科幻审计 fixture。
- [/] 建立玄幻审计 fixture。
- [/] 快照恢复 golden test：同一快照连续恢复 3 次 hash 一致。
- [ ] 事件日志重放测试：里程碑快照 + 事件重放等于最新快照。
- [ ] 手动编辑 dirty range 测试。
- [ ] 向量检索召回测试：Top 10 召回率 >= 0.85。
- [ ] 结构化查询准确率测试：准确率 >= 0.98。
- [ ] 摘要链追溯测试：定位误差不超过 1 章。
- [ ] VolumeBridge 回归测试：不违反 forbidden_contradictions。
- [ ] 后台任务暂停/继续测试。
- [ ] 后台任务取消测试。
- [x] 后台任务重试幂等测试。
- [ ] 同项目写任务并发锁测试。
- [ ] 全局扫描不阻塞 GUI 测试。

## 18. MVP 完成标准

- [x] 能创建小说项目并选择推理/科幻/玄幻题材。
- [/] 能完成参数填写、LLM 建议、世界观、角色、大纲生成。
- [x] 能连续生成至少 3 章。
- [x] 每章生成后能运行通用审计和题材审计。
- [x] 能更新对应题材追踪表。
- [x] 能保存和恢复章节快照。
- [/] 能查看审计报告和追踪表差异。
- [ ] 能安全暂停、取消、重试生成任务。
- [x] 能导出当前章节草稿。

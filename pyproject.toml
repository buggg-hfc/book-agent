[project]
name = "book-agent"
version = "0.1.0"
description = "MVP for an AI book-writing agent."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
